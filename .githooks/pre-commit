#!/bin/sh
# Runs before every commit: a broken piece entry never gets in.
npm run --silent check-pieces || { echo ""; echo "Commit blocked by check-pieces. Fix the pieces listed above and try again."; exit 1; }
